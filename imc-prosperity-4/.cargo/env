#!/bin/sh
# rustup shell setup
# affix colons on either side of $PATH to simplify matching
case ":${PATH}:" in
    *:"/Users/lakshaykumar/Documents/Playground/imc-prosperity-4/.cargo/bin":*)
        ;;
    *)
        # Prepending path in case a system-installed rustc needs to be overridden
        export PATH="/Users/lakshaykumar/Documents/Playground/imc-prosperity-4/.cargo/bin:$PATH"
        ;;
esac
